const Loading = () => {
    return (
        <div className="flex items-center justify-center h-screen z-50 bg-red-600">
            <h1>Loading...</h1>
        </div>
    );
};

export default Loading;
